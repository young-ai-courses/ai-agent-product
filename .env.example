# 把這個檔複製成 .env.local，再把你的 key 填進去（.env.local 不會進 git）
#
# key 去哪裡拿：https://console.groq.com → API Keys → Create API Key
#   · 免費，llama-3.3-70b 每天 1,000 次
#   · 它只會出現一次，產完馬上複製
#
# 部署在 Vercel 上的話，不要用這個檔 —— 加在
#   Vercel 專案 → Settings → Environment Variables → GROQ_API_KEY
# 加完要 Deployments → ⋯ → Redeploy 才生效
#
# 🔴 這串等於你的密碼：不要 commit、不要貼進 GitHub、不要傳給別人
GROQ_API_KEY=
